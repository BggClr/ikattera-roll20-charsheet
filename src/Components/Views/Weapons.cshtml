@model IList<Models.Weapon>

<div class="sheet-weapons">
	<div class="row">
		<div class="col-6">
			<div class="fancy-border">
				@foreach (var weapon in Model)
				{
					@await Html.PartialAsync("~/Views/CharacterFeature.cshtml", weapon)
				}
			</div>
		</div>
	</div>
</div>
